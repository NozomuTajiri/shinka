# マネジメントアバター「管理」(KANRI)

中間管理職の課題に寄り添うパートナーAI

## 概要

「管理」は、マネージャーの悩みに共感的に寄り添い、一緒に解決策を考える伴走型マネジメントコーチです。答えを教えるのではなく、質問を通じて気づきを促し、心理的安全性を確保しながら成長を支援します。

## ペルソナ

- **役割**: マネジメントコンサルタント
- **コミュニケーションスタイル**: 共感的で支援的
- **アプローチ**: 伴走型コーチング
- **原則**: 心理的安全性の確保

### 行動原則

1. まず共感し、受け止める
2. 質問で気づきを促す
3. 押し付けない
4. 具体的なアクションにつなげる

## 主な機能

### 1. 対話型コーチング

マネージャーの課題を聴き、共感的に応答しながら解決策を一緒に考えます。

```typescript
import { KanriAvatar } from './avatars/kanri/index.js';

const kanri = new KanriAvatar();
const session = kanri.startSession('manager-123', 'delegation');

const result = await kanri.processMessage(
  session.sessionId,
  '新人にタスクを任せたいのですが、どこまで任せていいか悩んでいます'
);

console.log(result.response);
// 「新人育成は悩みますよね。どんなタスクを考えていますか？」
console.log(result.framework);
// 'situational-leadership'
console.log(result.actionItems);
// ['タスクの複雑度を評価してみる', '本人の希望を聞いてみる']
```

### 2. 権限委譲プランの作成

タスクとチーム情報から、最適な委譲プランを自動生成します。

```typescript
// チームメンバー登録
kanri.addTeamMember(session.sessionId, {
  id: 'member-1',
  name: '田中太郎',
  role: 'ジュニアエンジニア',
  tenure: '6ヶ月',
  strengths: ['フロントエンド開発', 'コミュニケーション'],
  developmentAreas: ['設計スキル', '自律性'],
  motivators: ['新しいことに挑戦', '成長実感'],
  communicationStyle: '質問が多い、確認を求める',
  currentProjects: ['LP改修', 'コンポーネント実装'],
  oneOnOneNotes: [],
});

const plan = await kanri.createDelegationPlan(
  session.sessionId,
  'ユーザー一覧画面の新規作成',
  'APIからデータを取得して一覧表示する画面を作成'
);

console.log(plan);
// {
//   delegateTo: '田中太郎',
//   reason: 'フロントエンド開発が強み、成長機会として最適',
//   supportLevel: 'check-in',
//   milestones: [...],
//   successCriteria: ['期限内の完成', 'コードレビュー合格', '...'],
//   riskMitigation: ['週次チェックイン実施', '困った時の相談先明示']
// }
```

### 3. チーム開発プランの作成

チームの健全性スコアから、改善施策を提案します。

```typescript
const teamPlan = await kanri.createTeamDevelopmentPlan(
  session.sessionId,
  '開発チームA',
  {
    psychologicalSafety: 55,
    clarity: 70,
    engagement: 65,
    collaboration: 60,
    performance: 75,
  }
);

console.log(teamPlan.currentState.overallScore);
// 65
console.log(teamPlan.targetState.overallScore);
// 80 (3ヶ月後の目標)
console.log(teamPlan.initiatives);
// [
//   {
//     name: '心理的安全性ワークショップ',
//     objective: 'チームの発言しやすさを向上',
//     actions: ['ワークショップ開催', '振り返り会実施', 'サーベイ実施'],
//     ...
//   }
// ]
```

### 4. コンフリクト解決プランの作成

チーム内の対立を解決するための具体的なステップを提案します。

```typescript
const resolution = await kanri.createConflictResolutionPlan(
  ['田中さん', '佐藤さん'],
  'コードレビューの指摘方法で意見が対立しています'
);

console.log(resolution);
// {
//   nature: 'interpersonal',
//   rootCause: 'コミュニケーションスタイルの違い',
//   approach: '個別ヒアリング後、三者面談で相互理解を促進',
//   steps: [
//     { order: 1, action: '個別ヒアリング', timeline: '今週中', ... },
//     { order: 2, action: '共通点の整理', timeline: '来週', ... },
//     ...
//   ]
// }
```

### 5. 1on1アジェンダの自動生成

メンバーとの1on1のアジェンダを自動生成します。

```typescript
const agenda = await kanri.generateOneOnOneAgenda(
  session.sessionId,
  'member-1'
);

console.log(agenda);
// {
//   topics: [
//     '最近の仕事で嬉しかったこと・困ったこと',
//     '前回からのフォローアップ',
//     'キャリアや成長について',
//     '私（マネージャー）へのフィードバック'
//   ],
//   questions: [
//     '今一番気になっていることは何ですか？',
//     '理想の状態はどのようなものですか？',
//     ...
//   ],
//   previousActionItems: ['前回の1on1で決めたアクション']
// }
```

## マネジメントフレームワーク

KANRIは4つのマネジメントフレームワークを提供します：

### 1. 状況対応型リーダーシップ

メンバーの成熟度（スキル×モチベーション）に応じてリーダーシップスタイルを変える。

- **適用場面**: 新人育成、権限委譲、パフォーマンス管理
- **スタイル**: 指示型 / コーチ型 / 支援型 / 委任型

### 2. 心理的安全性の構築

チームメンバーが安心して発言・挑戦できる環境を作る。

- **適用場面**: チーム開発、コミュニケーション改善、失敗からの学習
- **ステップ**: 現状診断 → リーダー行動見直し → 施策実行

### 3. 効果的な権限委譲

成長を促しながら成果を出す委譲の方法。

- **適用場面**: 権限委譲、人材育成、業務効率化
- **ステップ**: タスク選定 → 適任者選定 → 実行とフォロー

### 4. 1on1ミーティングの極意

メンバーの成長を促す1on1の進め方。

- **適用場面**: 1on1、キャリア開発、モチベーション管理
- **ステップ**: 準備 → 傾聴と質問 → アクションと成長支援

## コーチング質問

7つのコーチング質問を3カテゴリに分類：

### Exploration（探索）

- 今一番気になっていることは何ですか？
- 理想の状態はどのようなものですか？

### Options（選択肢）

- どのような選択肢がありそうですか？
- 過去にうまくいった方法はありますか？

### Action（行動）

- 最初の一歩として何ができますか？
- 私からのサポートで必要なことはありますか？

### Reflection（振り返り）

- この対話を通じて気づいたことはありますか？

## 使用例：完全なセッション

```typescript
import { KanriAvatar } from './avatars/kanri/index.js';

async function managerCoachingSession() {
  const kanri = new KanriAvatar();

  // セッション開始
  const session = kanri.startSession('manager-123');

  // 対話1: 課題の共有
  const response1 = await kanri.processMessage(
    session.sessionId,
    'チームのモチベーションが下がっているように感じます'
  );
  console.log(response1.response);
  // 「それは心配ですね。どんな様子からそう感じましたか？」

  // 対話2: 深掘り
  const response2 = await kanri.processMessage(
    session.sessionId,
    'ミーティングで発言が少なく、元気がないように見えます'
  );
  console.log(response2.response);
  // 「なるほど。チームメンバーと1on1で話したことはありますか？」
  console.log(response2.framework);
  // 'psychological-safety'

  // チーム情報を追加
  kanri.addTeamMember(session.sessionId, {
    id: 'member-1',
    name: '田中太郎',
    role: 'エンジニア',
    tenure: '2年',
    strengths: ['技術力', '責任感'],
    developmentAreas: ['発信力'],
    motivators: ['新しい技術', '裁量'],
    communicationStyle: '控えめ',
    currentProjects: ['基盤改善'],
    oneOnOneNotes: [],
  });

  // チーム開発プラン作成
  const plan = await kanri.createTeamDevelopmentPlan(
    session.sessionId,
    '開発チーム',
    {
      psychologicalSafety: 50,
      clarity: 70,
      engagement: 55,
      collaboration: 60,
      performance: 65,
    }
  );

  console.log(`現在の総合スコア: ${plan.currentState.overallScore}`);
  console.log(`目標スコア: ${plan.targetState.overallScore}`);
  console.log('改善施策:', plan.initiatives.map(i => i.name));
}

managerCoachingSession();
```

## API Reference

### KanriAvatar

#### `startSession(managerId: string, challengeType?: string): KanriSession`

新しいセッションを開始します。

#### `processMessage(sessionId: string, userMessage: string): Promise<Result>`

ユーザーのメッセージを処理し、応答を生成します。

**Returns:**
```typescript
{
  response: string;              // AIの応答
  suggestedQuestion?: string;    // 次の質問の提案
  framework?: string;            // 推奨フレームワークID
  actionItems: string[];         // アクションアイテム
}
```

#### `addTeamMember(sessionId: string, member: TeamMember): void`

チームメンバー情報を追加します。

#### `createDelegationPlan(sessionId: string, taskName: string, taskDescription: string): Promise<DelegationPlan>`

権限委譲プランを作成します。

#### `createTeamDevelopmentPlan(sessionId: string, teamName: string, currentHealth: Partial<TeamHealth>): Promise<TeamDevelopmentPlan>`

チーム開発プランを作成します。

#### `createConflictResolutionPlan(parties: string[], conflictDescription: string): Promise<ConflictResolution>`

コンフリクト解決プランを作成します。

#### `generateOneOnOneAgenda(sessionId: string, memberId: string): Promise<Agenda>`

1on1のアジェンダを生成します。

## 技術仕様

- **AI Model**: Claude Sonnet 4 (`claude-sonnet-4-20250514`)
- **Max Tokens**: 600-1000（機能により異なる）
- **Language**: TypeScript（Strict mode）
- **Framework**: Anthropic SDK

## ファイル構成

```
src/avatars/kanri/
├── index.ts                    # メインクラス（567行）
├── management-framework.ts     # フレームワーク定義（291行）
├── types.ts                    # 型定義（130行）
└── README.md                   # このファイル
```

**Total**: 988行のTypeScriptコード、24のexport

## 開発ガイドライン

### 環境変数

```bash
ANTHROPIC_API_KEY=sk-ant-xxxxx
```

### TypeScriptチェック

```bash
npx tsc --noEmit src/avatars/kanri/*.ts
```

### 使用例の実行

```bash
npm run dev
```

## ユースケース

1. **新人育成の相談**
   - どこまで任せるか
   - どうサポートするか
   - 成長を促す方法

2. **チームビルディング**
   - 心理的安全性の構築
   - コミュニケーション改善
   - モチベーション管理

3. **コンフリクト解決**
   - 対立の仲裁
   - 相互理解の促進
   - 関係性の修復

4. **1on1の準備**
   - アジェンダ作成
   - 質問リスト
   - フォローアップ

5. **マネージャー自身の成長**
   - リーダーシップスタイルの見直し
   - 自己認識の向上
   - 行動変容の支援

## まとめ

「管理」は、マネージャーを孤独にしない伴走型AIコーチです。共感と質問を通じて、マネージャー自身が答えを見つけ、成長していくことを支援します。

---

🤖 **KANRI Avatar** - マネージャーのパートナー
